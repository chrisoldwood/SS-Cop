- Composite, e.g. C# style, C++ style

- Tables with an index

- Exclusion mechansim

- Change 'object'/'name' to 'identifier'

- Rule for no upper case acronyms

- Detect missing foreign keys by name, eg CustomerId in non Customer table

- Load rules into a table instead of using parameters

- Need interactive/batch mode like SQL-Unit (output + raiserror).

- Output rule when violation occurs
